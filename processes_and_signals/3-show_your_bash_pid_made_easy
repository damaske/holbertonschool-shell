#!/usr/bin/env bash

for p in /proc/[0-9]*/comm; do
    name=$(cat "$p" 2>/dev/null)
    if echo "$name" | grep -q "bash"; then
        pid=$(basename "$(dirname "$p")")
        echo "$pid $name"
    fi
done
